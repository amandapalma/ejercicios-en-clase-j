'use strict';
'use strict';

const numbers = [10, 11, 12, 13, 14, 15];

const foundIndex = numbers.indexOf(12);
console.log(foundIndex);

const notFoundIndex = numbers.indexOf(18);
console.log(notFoundIndex);
